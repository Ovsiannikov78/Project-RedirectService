# the property says that the DB scheme will be created by Hibernate while starting the app
spring.jpa.hibernate.ddl-auto=update


# the property says that spring will be printing all queries to the DB
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# configuration for MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/redirect-service?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=
spring.datasource.password=

#spring.datasource.username=redirect
#spring.datasource.password=redirect-pas      ======>>>> configuration for docker db

### configuration for Postgres
# the property says that the DB scheme will be created by Hibernate while starting the app

#spring.jpa.hibernate.ddl-auto=update

# the property says that spring will be printing all queries to the DB

#spring.jpa.show-sql=true
#spring.datasource.url=jdbc:postgresql://localhost:5432/redirect-service
#spring.datasource.username=postgres
#spring.datasource.password=1111


## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.flyway.baseline-on-migrate=true

server.port=8090

# Kafka configuration

app.topic=redirect-statistic

